#! /bin/bash

. /usr/src/mor/x6/framework/bash_functions.sh

# logs
mkdir -p /var/log/mor
chmod 777 /var/log/mor

# backups
mkdir -p /usr/local/mor/backups
chmod 777 /usr/local/mor/backups

touch /var/log/mor/db_backup.log
chmod 666 /var/log/mor/db_backup.log

report "Folders and permissions prepared" 0
